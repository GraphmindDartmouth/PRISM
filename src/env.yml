name: clip_diffusion_env
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - scikit-learn
  - pytorch
  - torchvision
  - torchaudio
  - cudatoolkit=11.8
  - pip:
      - transformers
      - diffusers
      - langchain
      - langchain-openai
      - openai
      - retry
      - lpips
      - xformers
      - tqdm
      - pillow
